name: Frontend Deploy

on:
  push:
    branches:
      - master
    paths:
      - "Frontend/manobhav-ui/**"
      - ".github/workflows/frontend-deploy.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: frontend-deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy:
    name: Build and deploy frontend
    runs-on: ubuntu-latest
    environment: Production

    defaults:
      run:
        working-directory: Frontend/manobhav-ui
    env:
      VITE_PUBLIC_API_BASE_URL: ${{ vars.VITE_PUBLIC_API_BASE_URL || secrets.VITE_PUBLIC_API_BASE_URL || 'https://api.manobhav.co.in' }}
      VITE_PUBLIC_ENABLE_VISITOR_ANALYTICS: ${{ vars.VITE_PUBLIC_ENABLE_VISITOR_ANALYTICS || secrets.VITE_PUBLIC_ENABLE_VISITOR_ANALYTICS || 'true' }}
      VITE_PUBLIC_ENABLE_FULL_VISITOR_CAPTURE: ${{ vars.VITE_PUBLIC_ENABLE_FULL_VISITOR_CAPTURE || secrets.VITE_PUBLIC_ENABLE_FULL_VISITOR_CAPTURE || 'false' }}
      VITE_PUBLIC_ANALYTICS_LEGAL_APPROVED: ${{ vars.VITE_PUBLIC_ANALYTICS_LEGAL_APPROVED || secrets.VITE_PUBLIC_ANALYTICS_LEGAL_APPROVED || 'false' }}
      VITE_PUBLIC_ANALYTICS_PRECISE_LOCATION: ${{ vars.VITE_PUBLIC_ANALYTICS_PRECISE_LOCATION || secrets.VITE_PUBLIC_ANALYTICS_PRECISE_LOCATION || 'false' }}
      VITE_PUBLIC_COGNITO_DOMAIN: ${{ vars.VITE_PUBLIC_COGNITO_DOMAIN || secrets.VITE_PUBLIC_COGNITO_DOMAIN || 'https://ap-south-2-gilvwjlxq.auth.ap-south-2.amazoncognito.com' }}
      VITE_PUBLIC_COGNITO_CLIENT_ID: ${{ vars.VITE_PUBLIC_COGNITO_CLIENT_ID || secrets.VITE_PUBLIC_COGNITO_CLIENT_ID || '6iukbgl3rpn34cks2f2l65kc3' }}
      VITE_PUBLIC_COGNITO_REDIRECT_URI: ${{ vars.VITE_PUBLIC_COGNITO_REDIRECT_URI || secrets.VITE_PUBLIC_COGNITO_REDIRECT_URI || 'https://manobhav.co.in/callback' }}
      VITE_PUBLIC_COGNITO_LOGOUT_URI: ${{ vars.VITE_PUBLIC_COGNITO_LOGOUT_URI || secrets.VITE_PUBLIC_COGNITO_LOGOUT_URI || 'https://manobhav.co.in' }}
      VITE_PUBLIC_COGNITO_SCOPES: ${{ vars.VITE_PUBLIC_COGNITO_SCOPES || secrets.VITE_PUBLIC_COGNITO_SCOPES || 'openid email phone profile' }}
      VITE_PUBLIC_ADMIN_GROUP: ${{ vars.VITE_PUBLIC_ADMIN_GROUP || secrets.VITE_PUBLIC_ADMIN_GROUP || 'Admin' }}
      AWS_REGION: ${{ vars.AWS_REGION || secrets.AWS_REGION || 'ap-south-2' }}
      S3_BUCKET: ${{ vars.S3_BUCKET || secrets.S3_BUCKET }}
      CF_DISTRIBUTION_ID: ${{ vars.CF_DISTRIBUTION_ID || secrets.CF_DISTRIBUTION_ID }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: Frontend/manobhav-ui/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm run test:run

      - name: Validate public frontend environment
        shell: bash
        run: |
          required_vars=(
            VITE_PUBLIC_API_BASE_URL
            VITE_PUBLIC_ENABLE_VISITOR_ANALYTICS
            VITE_PUBLIC_ENABLE_FULL_VISITOR_CAPTURE
            VITE_PUBLIC_ANALYTICS_LEGAL_APPROVED
            VITE_PUBLIC_ANALYTICS_PRECISE_LOCATION
            VITE_PUBLIC_COGNITO_DOMAIN
            VITE_PUBLIC_COGNITO_CLIENT_ID
            VITE_PUBLIC_COGNITO_REDIRECT_URI
            VITE_PUBLIC_COGNITO_LOGOUT_URI
            VITE_PUBLIC_COGNITO_SCOPES
            VITE_PUBLIC_ADMIN_GROUP
          )

          missing=()
          for key in "${required_vars[@]}"; do
            if [ -z "${!key}" ]; then
              missing+=("$key")
            fi
          done

          if [ "${#missing[@]}" -gt 0 ]; then
            printf 'Missing required public frontend env vars: %s\n' "${missing[*]}" >&2
            exit 1
          fi

      - name: Build
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Upload immutable assets
        run: |
          if [ -d dist/assets ]; then
            aws s3 cp dist/assets "s3://${S3_BUCKET}/assets" \
              --recursive \
              --cache-control "public,max-age=31536000,immutable"
          fi

      - name: Upload short-cache public files
        run: |
          aws s3 cp dist "s3://${S3_BUCKET}" \
            --recursive \
            --exclude "index.html" \
            --exclude "assets/*" \
            --cache-control "public,max-age=300,must-revalidate"

      - name: Upload no-cache index
        run: |
          aws s3 cp dist/index.html "s3://${S3_BUCKET}/index.html" \
            --cache-control "no-cache,no-store,must-revalidate" \
            --content-type "text/html"

      - name: Invalidate CloudFront
        run: |
          aws cloudfront create-invalidation \
            --distribution-id "${CF_DISTRIBUTION_ID}" \
            --paths "/*"
